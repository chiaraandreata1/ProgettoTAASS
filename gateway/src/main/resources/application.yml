spring:
  application:
    name: zuul-server

server:
  port: 8080

eureka:
#  instance:
#    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'

#ribbon:
#  eureka:
#    enabled: true
---
#spring:
#  profiles: docker

#eureka:
#  client:
#    service-url:
#      defaultZone: http://service-discovery:8761/eureka

zuul:
  ignoredServices: '*'
  routes:
    tournaments-service: /api/v1/tournaments/**
    reservation-service: /api/v1/reservations/**
    board-service: /api/v1/boards/**
    lesson-service: /api/v1/lessons/**
    user-service: /api/v1/users/**
    frontend:
      path: /*
      url: http://localhost:4200/
#  routes:
#    tournaments-service: /myusers/**